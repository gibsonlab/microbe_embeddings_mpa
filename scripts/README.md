# Instructions

Each sub-directory has its own separate set of instructions.
Most, if not all, parts of the pipeline requires the accompanying `gem` package to be discoverable.

From the project root (the parent directory containing the `gem` folder), please do the following:
```bash
# Prerequisite (Linux): `gem` appears as a subdirectory when we do `ls` 
export PYTHONPATH=$(pwd)
```
